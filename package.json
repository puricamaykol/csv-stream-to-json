{
  "name": "csv-stream-to-json",
  "version": "1.0.1",
  "description": "Takes a CSV file stream and parse it line by line to JSON. It accepts two callbacks: 1. Called for each file line, 2. Called at the end of the file parsing and gets passed the whole json array",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "node",
    "stream",
    "csv",
    "json",
    "csvtojson"
  ],
  "author": "Maykol Purica <puricamaykol@gmail.com>",
  "license": "ISC"
}
